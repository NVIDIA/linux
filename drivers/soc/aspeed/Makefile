# SPDX-License-Identifier: GPL-2.0-only
obj-$(CONFIG_ASPEED_BMC_DEV)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-bmc-dev.c))
obj-$(CONFIG_ASPEED_HOST_BMC_DEV)	+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-host-bmc-dev.c))
obj-$(CONFIG_ASPEED_LPC_CTRL)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-lpc-ctrl.c))
obj-$(CONFIG_ASPEED_LPC_SNOOP)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-lpc-snoop.c))
obj-$(CONFIG_ASPEED_LPC_MAILBOX)	+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-lpc-mbox.c))
obj-$(CONFIG_ASPEED_LPC_PCC)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-lpc-pcc.c))
obj-$(CONFIG_ASPEED_UDMA)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-udma.c))
obj-$(CONFIG_ASPEED_UART_ROUTING)	+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-uart-routing.c))
obj-$(CONFIG_ASPEED_SSP)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-ssp.c))
obj-$(CONFIG_ASPEED_P2A_CTRL)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-p2a-ctrl.c))
obj-$(CONFIG_ASPEED_SOCINFO)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-socinfo.c))
obj-$(CONFIG_ASPEED_SBC)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-sbc.c))
obj-$(CONFIG_ASPEED_XDMA)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-xdma.c))
obj-$(CONFIG_ASPEED_RVAS)		+= rvas/
obj-$(CONFIG_ARCH_ASPEED)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-usb-phy.c))
obj-$(CONFIG_ARCH_ASPEED)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-usb-hp.c))
obj-$(CONFIG_ASPEED_PCIE_MMBI)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-pcie-mmbi.c))
obj-$(CONFIG_ASPEED_DISP_INTF)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-disp-intf.c))
obj-$(CONFIG_ASPEED_MBOX)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-mbox.c))
obj-$(CONFIG_ASPEED_MCTP)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/aspeed-mctp.c))
obj-$(CONFIG_AST2600_OTP)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/ast2600-otp.c))
obj-$(CONFIG_AST2700_OTP)		+= $(patsubst $(src)/%.c,%.o,$(wildcard $(src)/ast2700-otp.c))
obj-y					+= espi/
